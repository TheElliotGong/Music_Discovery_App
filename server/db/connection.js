/**
 * Deprecated JS entrypoint (kept for compatibility).
 *
 * The database connection module has migrated to TypeScript. Import from connection.ts instead.
 */
export { connect, disconnect } from './connection.ts';
